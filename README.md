# onlinemarket.work
Zend Framework 2: Fundamentals New

## instruções do pull:
utilizado o "git commit" para fazer o primeiro commit, logo após, foi configurado o repositório do github, através do "git remote add origin"
feito isso, foi feito upload para o repositório através do "git push"
também foram feitos outros commits, e o repositório atualizado através do "git pull"